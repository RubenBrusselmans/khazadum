apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: renovate
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infrastructure/renovate
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
