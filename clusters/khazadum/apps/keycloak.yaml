apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: keycloak
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./apps/keycloak
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  prune: true
  force: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
