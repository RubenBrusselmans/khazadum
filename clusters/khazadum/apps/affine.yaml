apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: affine
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./apps/affine
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  prune: true
  force: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
