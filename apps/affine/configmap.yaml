apiVersion: v1
kind: ConfigMap
metadata:
  name: affine-config
  namespace: affine
data:
  config.json: |
    {
      "copilot": {
        "enabled": true,
        "byok": {
          "enabled": true,
          "allowCustomEndpoint": true,
          "allowPrivateEndpoint": true
        }
      }
    }
